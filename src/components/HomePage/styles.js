import colors from '../../utils/colors';

const styles = {
    container: {
        display: 'flex',
        flexDirection: 'column'
    },
    total: {
        marginBottom: '5rem',
        color: colors.gallery,
        alignSelf: 'center'
    }
};

export default styles;
